version: '3.4'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "80:8080"

  db:
    image: postgres:12.1
    environment:
      POSTGRES_PASSWORD: example
      POSTGRES_USER: example
    volumes:
      - db_data:/var/lib/postgresql/data







volumes:
  db_data: